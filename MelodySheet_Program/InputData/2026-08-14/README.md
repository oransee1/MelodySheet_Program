# InputData/2026-08-14

프로그램 개발을 시작한 날의 입력이다. `test_render.py` 가 **이 날짜의 Input01** 을 연다.

- `Input01/` — Sunday Slow Motion 전체 세트 (pdf, mp3, mid, musicxml, ly, gp5)
- `Input02/` — 빈 자리

교차검증 숫자(90마디, 202.723초, 현악 약 +2초 지연)는 이 세트에서 나왔다. 엔진을 고친 뒤 같은 숫자로 회귀하는 기준 입력이다.

다음 날 폴더 `2026-08-15` 는 같은 곡을 다시 넣은 작업 복사본이다.
